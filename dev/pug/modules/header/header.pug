header
    if(pageClass === 'sign-up-page')
        include ../../modules/header/sign-up
    else if(pageClass === 'walkthrough-page')
        include ../../modules/header/walkthrough
    else if (pageClass === 'home-page')
        include ../../modules/header/home

